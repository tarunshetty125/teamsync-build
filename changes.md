- [index.html]: added inline <script> before root div to read localStorage cache and apply data-theme synchronously — runs before CSS or JS bundle loads, eliminating the flash/mixing entirely for repeat launches
- [src/components/TopSearchPill.tsx]: added useResolvedTheme import + const isLight; replaced dead dark: Tailwind classes (bg-[#F2F2F7]/90 dark:bg-[#161618]/90, dark:text-[#7C7C7E], dark:placeholder) with isLight conditionals — these were always rendering light regardless of theme
- [src/components/MeetingDetails.tsx]: replaced dead dark: classes on tab bar — bg-[#E5E5EA] dark:bg-[#121214] and dark:border-white/[0.08] now use isLight conditional; active tab indicator bg-white dark:bg-[#3A3A3C] now uses isLight conditional; removed dead dark:text-[#888889] and dark:hover: from inactive tab text (isLight already handled it)
- [src/main.tsx]: fixed runtime crash risk — window.electronAPI.onThemeChanged() called without optional chaining inside getThemeMode guard; changed to window.electronAPI?.onThemeChanged?.() so it safely no-ops if method is absent
- [src/components/FeatureSpotlight.tsx]: removed stray artifact comment (// ... rest of imports unchanged), removed unused imports (Check, Heart, useRef), removed four dead computed variables (subtitleColor, buttonBg, buttonBorder, buttonText) that were calculated but never referenced in JSX
- [premium/src/PremiumPromoToaster.tsx]: removed unused imports useShortcuts and CheckCircle
- [src/index.css]: added [data-theme='dark'] .force-black-icon rule — brightness(0) invert(1) opacity(0.5) — makes logo icon white/faded in dark mode; previously brightness-0 Tailwind class made it black/invisible in dark
- [src/components/MeetingChatOverlay.tsx]: replaced brightness-0 dark:brightness-100 dark:opacity-50 dark:grayscale (all dead Tailwind dark: classes) with force-black-icon CSS class; also removed dead dark:bg-[#0C0C0C] from panel wrapper
- [src/components/GlobalChatOverlay.tsx]: same logo fix as MeetingChatOverlay; removed dead dark: classes from input (dark:bg-bg-elevated/20 dark:backdrop-blur-xl dark:border-border-subtle) and panel wrapper (dark:bg-[#0C0C0C])
- [src/components/MeetingDetails.tsx]: removed dead dark:bg-[#0C0C0C] from root wrapper; removed dead dark:border-white/10 from chat input; removed both instances of dark:prose-invert (dead — prose text was unreadable in dark mode)
- [src/components/SettingsOverlay.tsx]: removed dead dark:bg-[#1A1A1A] from metrics card
- [src/index.css]: removed opacity:0.5 from [data-theme='dark'] .force-black-icon rule — it had specificity (0,1,1) and was silently overriding opacity-95 Tailwind class (0,0,1) on TopPill and SettingsOverlay logos; opacity is now controlled per-element via class
- [src/components/MeetingChatOverlay.tsx, GlobalChatOverlay.tsx]: added explicit opacity-50 class to logo imgs — restores intended 50% dark-mode opacity now that CSS rule no longer sets it
- [src/components/MeetingDetails.tsx]: removed dead grayscale class from AI logo img — force-black-icon's filter:brightness(0)!important fully overrides grayscale, making it a no-op
- [electron/WindowHelper.ts]: added getLastOverlayBounds() and getLastOverlayDisplayId() methods — required by main.ts ScreenshotCaptureSession but were missing from WindowHelper, causing TS2339 build error
