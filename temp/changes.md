- [electron/main.ts]: Section 00 — added STT routing: final recruiter (system audio) transcripts now call `feedInterviewerUtterance()` on the knowledge orchestrator directly from the `createSTTProvider()` transcript handler
- [premium/electron/knowledge/types.ts]: Section 01 — appended Live Negotiation Coaching types: NegotiationPhase, OfferEvent, OfferState, NegotiationState, LiveCoachingResponse
- [premium/electron/knowledge/ContextAssembler.ts]: Section 01 — imported LiveCoachingResponse, extended PromptAssemblyResult with optional liveNegotiationResponse field
- [premium/electron/knowledge/NegotiationConversationTracker.ts]: Section 02 — created new file implementing negotiation state machine with salary extraction, phase transitions, silence timer gating, and deep copy getState()
- [premium/electron/knowledge/LiveNegotiationAdvisor.ts]: Section 03 — created new file implementing LLM-powered live coaching response generator with 5-second timeout and fallback
- [premium/electron/knowledge/KnowledgeOrchestrator.ts]: Section 04 — integrated NegotiationConversationTracker and LiveNegotiationAdvisor: added tracker field, seeding from pre-computed script, recruiter utterance routing in feedInterviewerUtterance(), user utterance feed in processQuestion(), live negotiation short-circuit path, getNegotiationTracker(), resetNegotiationSession(), and tracker reset in deleteDocumentsByType()
- [premium/electron/knowledge/ContextAssembler.ts]: Section 05 — added buildLiveNegotiationSystemPrompt() export function
- [electron/ipcHandlers.ts]: Section 06 — added profile:get-negotiation-state and profile:reset-negotiation IPC handlers
- [electron/preload.ts]: Section 06 — added profileGetNegotiationState and profileResetNegotiation to ElectronAPI interface and contextBridge
- [src/types/electron.d.ts]: Section 06 — added profileGetNegotiationState and profileResetNegotiation to ElectronAPI type declarations
- [premium/src/NegotiationCoachingCard.tsx]: moved from src/components — live negotiation coaching UI card is a premium-only component
- [src/premium/index.tsx]: registered NegotiationCoachingCard via import.meta.glob with NullComponent fallback for open-source builds
- [src/components/NativelyInterface.tsx]: updated NegotiationCoachingCard import to use ../premium barrel; Section 08 — extended Message interface with negotiation coaching fields, added negotiation coaching JSON detection in onGeminiStreamDone and onRAGStreamComplete handlers, added NegotiationCoachingCard render in renderMessageText()
- [electron/LLMHelper.ts]: LLMHelper wiring — added liveNegotiationResponse short-circuit in both chatWithGemini() and streamChat() knowledge mode intercepts to bypass second LLM call
- [premium/electron/knowledge/KnowledgeOrchestrator.ts]: fix — deleteDocumentsByType now only resets negotiationTracker on RESUME delete, not JD delete (prevents wiping live negotiation on JD change)
- [premium/electron/knowledge/KnowledgeOrchestrator.ts]: fix — generateNegotiationScriptOnDemand now seeds negotiationTracker.setUserTarget() after saving the script
- [premium/electron/knowledge/KnowledgeOrchestrator.ts]: fix — AOT pipeline .then() handler seeds negotiationTracker with script target after async completion
- [premium/electron/knowledge/NegotiationConversationTracker.ts]: fix — extractAmounts now extracts both bounds from "between X and Y" patterns (match[2] when present)
- [premium/electron/knowledge/LiveNegotiationAdvisor.ts]: fix — replaced non-existent resume.identity.current_role with resume.experience[0].role (IdentityInfo has no current_role field)
- [src/components/SettingsOverlay.tsx]: added Beta pill next to Company Intel header and disclaimer at bottom of dossier results
- [electron/LLMHelper.ts]: fix structured generation provider order — moved Claude after Gemini Pro/Flash; Claude's non-streaming messages.create fails on large payloads causing wasted latency before Gemini fallback
- [electron/WindowHelper.ts, electron/main.ts]: fix focus steal on shortcuts — toggleMainWindow and all screenshot-restore paths now use showInactive() (pass inactive:true) so keyboard shortcuts never steal OS focus from the active app
- [electron/LLMHelper.ts, premium/electron/knowledge/KnowledgeOrchestrator.ts]: senior review fix — replaced feedInterviewerUtterance(message) with new feedForDepthScoring(message) in both non-streaming and streaming knowledge intercepts; feedInterviewerUtterance also calls addRecruiterUtterance() which was misclassifying user questions as recruiter offers and prematurely activating the negotiation tracker from INACTIVE; added feedForDepthScoring() method to KnowledgeOrchestrator that only calls depthScorer.addUtterance(); streaming path also now gets depth scorer data (was previously absent entirely)
- [src/components/NativelyInterface.tsx]: senior review fix — changed m === msg to m.id === msg.id in onSilenceTimerEnd callback; object identity check always failed after any state update (stream chunk, completion, error) because messages array is reconstructed via spread, invalidating the original msg reference before the 5-second timer fires; silence timer bar would freeze permanently in real usage
- [premium/electron/knowledge/LiveNegotiationAdvisor.ts]: wired buildLiveNegotiationSystemPrompt() — imported from ContextAssembler and prepended as a separate content part in the generateContentFn call; data prompt cleaned of duplicate role/rules framing that is now exclusively in the system prefix; separating directive constraints (JSON format, brevity, no placeholders) from the data context improves LLM format adherence under the 5-second timeout
- [electron/WindowHelper.ts]: fix focus steal — removed setAlwaysOnTop(true,"floating") from switchToOverlay() show path; it was being called after showInactive() and triggers [NSApp activateIgnoringOtherApps] on macOS, undoing the inactive show. Level is set once at window creation and persists across hide/show cycles.
- [electron/main.ts]: fix focus steal — removed both targetFocusWindow.focus() calls from stealth mode debounce handler; dock.hide()/dock.show() manage app presence, focus must never be stolen from the user's active app
- [electron/ModelSelectorWindowHelper.ts]: fix focus steal — removed mainWin.focus() from hideWindow() (closed dropdown was stealing focus back to Natively); added guard to only call setAlwaysOnTop when value actually changes
- [electron/main.ts]: PR#103 — added _isQuitting flag + isQuitting()/setQuitting() public methods; added getIsMeetingActive() accessor; added broadcastMeetingState() that sends 'meeting-state-changed' event; wired broadcastMeetingState() into startMeeting() and endMeeting(); added setQuitting(true) in before-quit handler
- [electron/WindowHelper.ts]: PR#103 — added showOverlay()/hideOverlay() independent control methods; added close-to-tray for Windows/Linux (intercept close event and hide when not quitting); added isMeetingActive guard in switchToLauncher() to block switch while meeting is active; added opacity=0 flash prevention in hideMainWindow() on Win32; added 'ensure-expanded' IPC send in switchToOverlay()
- [electron/ipcHandlers.ts]: PR#103 — added show-overlay, hide-overlay, get-meeting-active IPC handlers
- [electron/preload.ts]: PR#103 — added showOverlay, hideOverlay, getMeetingActive, onMeetingStateChanged, onEnsureExpanded to ElectronAPI interface and contextBridge
- [src/types/electron.d.ts]: PR#103 — added showOverlay, hideOverlay, getMeetingActive, onMeetingStateChanged, onEnsureExpanded to ElectronAPI type declarations
- [src/components/Launcher.tsx]: PR#103 — added isMeetingActive state; subscribes to getMeetingActive() on mount and onMeetingStateChanged events; shows "Meeting in progress — Resume" banner above Start Natively button when meeting is active; banner calls showOverlay() to return to overlay
- [src/components/ui/TopPill.tsx]: PR#103 — added optional onLogoClick prop wired to the logo button
- [src/components/NativelyInterface.tsx]: PR#103 — added onEnsureExpanded listener (sets isExpanded=true); passes onLogoClick to TopPill (calls hideOverlay() to hide without ending meeting)
- [electron/WindowHelper.ts]: review fix — removed incorrect opacity=0 from hideMainWindow(); flash-prevention belongs in show paths (already handled by switchToOverlay/switchToLauncher win32+contentProtection paths), not before hide() where it permanently corrupts opacity for all subsequent non-contentProtection show calls
- [electron/WindowHelper.ts]: review fix — overlay close handler now handles meeting-active case explicitly: calls hideOverlay() instead of switchToLauncher() when a meeting is running, preventing the deadlock where switchToLauncher's isMeetingActive guard would silently no-op leaving the overlay undismissable
- [src/components/Launcher.tsx]: review fix — getMeetingActive() async call now guarded by mounted ref to prevent setState on unmounted component; cleanup sets mounted=false
- [src/components/Launcher.tsx]: review fix — Resume banner now calls showWindow() instead of showOverlay(); user explicitly clicked Resume so the overlay must focus and become fully visible (showOverlay was showInactive which would silently appear behind other apps)
- [electron/WindowHelper.ts]: removed isMeetingActive guard from switchToLauncher(); the overlay close handler already guards the specific auto-switch case; blanket guard was blocking explicit user navigation (logo click → launcher)
- [src/components/NativelyInterface.tsx]: fixed logo click — setWindowMode('launcher') instead of hideOverlay(); clicking the Natively logo now actually switches to the launcher
- [src/components/Launcher.tsx]: unified CTA button — merged banner + Start Natively into one jelly pill that morphs; idle=blue gradient with logo+text, meeting=green gradient with ping live-dot+Meeting ongoing; gradient layers crossfade via opacity transition; content crossfades via AnimatePresence y-slide; all jelly styling (highlight band, shadow, hover glow) shared between both states
- [src/components/Launcher.tsx]: critical fix — "Meeting ongoing" button changed from showWindow() to setWindowMode('overlay'); showWindow() delegates to showMainWindow() which checks currentWindowMode, but logo-click sets currentWindowMode='launcher' so showWindow() would re-show launcher instead of returning to overlay; setWindowMode('overlay') forces switchToOverlay() unconditionally
- [electron/WindowHelper.ts]: updated stale comment in overlay close handler (no longer references removed meeting guard)
- [electron/WindowHelper.ts, electron/ipcHandlers.ts, electron/preload.ts, src/types/electron.d.ts]: threaded inactive param through setWindowMode chain; setWindowMode(mode, inactive?) now passes inactive to switchToLauncher/switchToOverlay
- [src/components/Launcher.tsx]: Meeting ongoing button now calls setWindowMode('overlay', true) — inactive=true means overlay uses showInactive() so the Natively app is not activated/focused; launcher hides, overlay floats on top stealthily without stealing OS focus or making Natively detectable
- [src/components/NativelyInterface.tsx]: review fix — onEnsureExpanded handler now sets isStealthRef.current=true before setIsExpanded(true); if isExpanded was false when ensure-expanded fires, the isExpanded effect would call showWindow(isStealthRef.current); without this fix it would call showWindow(false) → show()+focus() undoing the inactive=true stealth from switchToOverlay
- [electron/tsconfig.json]: PR#105 — reverted outDir to "../dist-electron" (original correct value); PR proposed "../dist-electron/electron" but that is wrong for this repo: because premium/electron/**/*.ts is in the include array, tsc infers rootDir as the project root (common ancestor of electron/ and premium/electron/), which causes the output to already land in dist-electron/electron/; changing outDir to dist-electron/electron would produce double-nesting at dist-electron/electron/electron/ breaking all __dirname-relative paths in WindowHelper.ts and LocalEmbeddingProvider.ts
- [package.json]: PR#105 — added "build:electron" script; refactored electron:dev, electron:build, app:build to delegate to build:electron instead of inlining the tsc command
- [.github/workflows/build-smoke.yml]: PR#105 — new CI smoke test that builds renderer + electron main process and asserts dist-electron/electron/main.js exists
- [scripts/build-native.js]: PR#106 — refactored macOS build to default to current-arch only (set NATIVELY_BUILD_ALL_MAC_ARCHES=1 for dual x64+arm64); added verifyArtifacts() that throws on missing .node files after build (fail-fast); per-target rustup install moved inside loop with per-target try/catch; added win32/linux artifact verification map for non-macOS platforms
- [package.json]: PR#106 — added npm run build:native to app:build pipeline (conflict-resolved against PR#105's build:electron abstraction); packaged builds now always recompile native addon before electron-builder runs
- [package.json]: senior review fix — app:build now passes cross-env NATIVELY_BUILD_ALL_MAC_ARCHES=1 to build:native; electron-builder targets both x64 and arm64 for macOS so the packaging pipeline must always build both arches; without this, arm64 CI would only build arm64 and electron-builder would silently consume a stale x64 artifact for the x64 package
- [native-module/src/microphone.rs]: PR#107 — added resolve_input_device() that honors user-selected microphone: exact match → case-insensitive fallback → hard error listing available devices; MicrophoneStream::new now passes device_id.as_deref() to resolver instead of silently ignoring the parameter (_device_id stub activated)
- [native-module/src/microphone.rs]: senior review fix — filter for "default" sentinel changed from *id != "default" (case-sensitive) to !id.eq_ignore_ascii_case("default"); prevents "Default" or "DEFAULT" from bypassing the sentinel and being sent into device lookup where they would always fail with "device not found"
- [electron/main.ts]: PR#109 — added systemPreferences import; removed isDev guard from logToFile so packaged builds write audio diagnostics to disk; added ensureMacMicrophoneAccess() that checks/requests macOS mic permission before audio operations; converted startAudioTest() to async with permission gate, multi-window broadcast (settings + launcher + overlay), correct listener-before-start ordering, and device fallback (preferred → default); fixed event name audio-level → audio-test-level to match the preload's onAudioTestLevel channel; added permission check at top of startMeeting() that broadcasts meeting-audio-error and throws before isMeetingActive is set
- [electron/ipcHandlers.ts]: PR#109 — added await to appState.startAudioTest(deviceId) so the now-async method is properly awaited before returning { success: true }
- [src/components/SettingsOverlay.tsx]: PR#109 — replaced Web Audio API microphone level monitoring (getUserMedia + AudioContext + AnalyserNode + RAF loop) with native IPC path: onAudioTestLevel for level events, startAudioTest(selectedInput) to start, stopAudioTest() to stop; removed audioContextRef/analyserRef/sourceRef/rafRef/streamRef refs; device IDs now consistent between settings test and live meeting runtime
- [electron/verboseLog.ts]: new file — module-level singleton (isVerboseLogging / setVerboseLoggingFlag) that decouples the verbose flag from AppState so SessionTracker and other non-AppState modules can check it without circular imports
- [electron/main.ts]: added _verboseLogging field to AppState; loaded from SettingsManager on boot; added getVerboseLogging() / setVerboseLogging() methods; wired setVerboseLoggingFlag() on init and on change; gated PR#110 audio pipeline diagnostic log behind _verboseLogging in startMeeting() setTimeout handler
- [electron/ipcHandlers.ts]: added get-verbose-logging and set-verbose-logging IPC handlers
- [electron/preload.ts]: added getVerboseLogging / setVerboseLogging to ElectronAPI interface and contextBridge
- [src/types/electron.d.ts]: added getVerboseLogging / setVerboseLogging to ElectronAPI type declarations
- [src/components/SettingsOverlay.tsx]: added verboseLogging state; loads on settings open via getVerboseLogging(); added "Verbose debug logging" toggle (amber-accented Terminal icon) in General settings above Interviewer Transcript toggle
- [electron/SessionTracker.ts]: PR#110 — added user segment logging mirroring existing interviewer sampling, gated behind isVerboseLogging() so it's suppressed by default
- [native-module/index.js]: PR#110 — added startup log showing whether native addon loaded from local bundled binary or platform package (unconditional one-time init log)
- [electron/services/SettingsManager.ts]: added verboseLogging?: boolean to AppSettings interface so SettingsManager.get/set type-checks correctly
- [electron/SessionTracker.ts]: gated existing interviewer segment sampling log behind isVerboseLogging() (was always-on, making toggle appear to have no effect)
- [electron/main.ts]: gated setupSystemAudioPipeline() STT rate config logs and "Full Audio Pipeline Initialized" log behind _verboseLogging (same fix — were always-on)
- [premium/electron/knowledge/NegotiationConversationTracker.ts]: P0-1 — exported escapeXml(); applied escapeXml() to e.raw in getStateXML() offer history; added MAX_UTTERANCE_LENGTH=500 constant with truncation in addRecruiterUtterance/addUserUtterance; added 'falling' to trajectory assignment (amount < prev case); deep-copied OfferEvent objects in getState()
- [premium/electron/knowledge/LiveNegotiationAdvisor.ts]: P0-1 — imported escapeXml and applied to userQuestion interpolation in prompt; P1-1 — replaced bare setTimeout/Promise.race with AbortController + clearTimeout-in-finally to prevent timer handle leak; P2-3 — added PHASE_LABELS map so fallback tacticalNote surfaces user-friendly copy instead of raw enum names
- [premium/electron/knowledge/KnowledgeOrchestrator.ts]: P1-2 — added else-if DocType.JD branch in deleteDocumentsByType() that resets negotiationTracker (prevents cross-session phase/offer bleed when JD is replaced); P2-5 — removed constructor-time tracker seed from cached script (stale data from prior session contaminated fresh negotiations)
- [premium/electron/knowledge/types.ts]: P2-4 — added 'falling' to OfferState.trajectory union type
- [electron/rag/vectorSearchWorker.ts]: P1-4 — added integer validation (Number.isInteger, 1–65536) for dim before table name interpolation in both nativeVecSearch and nativeVecSearchSummaries handlers; invalid dim posts error and breaks without executing SQL
- [electron/main.ts]: P2-1 — added LOG_MAX_BYTES=10MB constant and log rotation in logToFile(): renames current file to .log.1 and removes old .log.1 when size limit is reached; P2-12 — added _audioTestStarting boolean guard and refactored startAudioTest() to delegate to private _startAudioTestImpl() preventing concurrent calls both passing the async permission gate
- [electron/rag/EmbeddingPipeline.ts]: P2-8 — added UPDATE meetings SET embedding_provider/embedding_dimensions write in embedMeetingSummary() mirroring embedChunk(); prevents summary-only meetings from being silently excluded by provider-switch compatibility checks
- [premium/electron/knowledge/ContextAssembler.ts]: P2-10 — imported escapeXml from NegotiationConversationTracker; applied to candidateName before interpolation into knowledge_engine_rules system prompt block
- [src/components/NativelyInterface.tsx]: P3-7 — removed redundant phase={...as any} override in NegotiationCoachingCard render; phase is already spread via {...msg.negotiationCoachingData} and the component is React.FC<any>
- [electron/tsconfig.json]: PR#112 — changed outDir from "../dist-electron" to "../dist-electron/electron"; aligns tsc output with package.json "main" field which already points to dist-electron/electron/main.js
- [electron/ModelSelectorWindowHelper.ts]: PR#112 — added WindowActivationOptions type; updated showWindow() to accept options param with activate flag; uses showInactive() instead of show() and skips focus() when activate=false
- [electron/SettingsWindowHelper.ts]: PR#112 — added WindowActivationOptions type; updated showWindow() to accept options param with activate flag; same showInactive/focus pattern as ModelSelectorWindowHelper
- [electron/WindowHelper.ts]: PR#112 — added lastOverlayBounds/lastOverlayDisplayId private fields; added clampBoundsToWorkArea(), rememberOverlayBounds(), getDefaultOverlayBounds(), getOverlayBoundsForShow() private helpers; added getLastOverlayBounds()/getLastOverlayDisplayId() public accessors; refactored setOverlayDimensions() to use clampBoundsToWorkArea() with per-display workArea instead of primaryDisplay; added move/resize event listeners on overlayWindow to track bounds; updated switchToOverlay() to use getOverlayBoundsForShow() instead of hardcoded centering
- [electron/ScreenshotHelper.ts]: PR#112 — added preferredDisplay param to captureWithDesktopCapturer(); added captureStitchedDesktopArea() helper; updated getScreenshotCommand() to Linux-only (macOS now uses desktopCapturer); refactored takeScreenshot() to remove hideMainWindow/showMainWindow callbacks and isCapturing guard (guard moved to main.ts); refactored takeSelectiveScreenshot() to remove callbacks; macOS now routes through desktopCapturer for both full and selective captures
- [electron/main.ts]: PR#112 — added screen to Electron imports; added ScreenshotCaptureSession interface and ScreenshotWindowMode/ScreenshotCaptureKind types; added screenshotCaptureInProgress guard flag; added createScreenshotCaptureSession(), getDisplayById(), getTargetDisplayForFullScreenshot(), hideWindowsForScreenshot(), restoreWindowsAfterScreenshot(), withScreenshotCaptureSession() private methods; replaced takeScreenshot()/takeSelectiveScreenshot() with session-based pattern; error filter now also suppresses "Screenshot capture already in progress"
- [src/hooks/useShortcuts.ts]: PR#112 — added captureAndProcess to ShortcutConfig interface, DEFAULT_SHORTCUTS map, mapBackendToFrontend if-chain, and updateShortcut switch statement
- [src/components/SettingsOverlay.tsx]: PR#112 — added "Capture Screen & Ask AI" shortcut row (Sparkles icon) after processScreenshots row
- [src/components/NativelyInterface.tsx]: PR#112 — updated 4 screenshot catch blocks to suppress "Screenshot capture already in progress" sentinel errors (double-tap guard noise reduction)
- [electron/main.ts]: senior review fix — restoreWindowsAfterScreenshot: changed shouldRestoreMainWindow to use only session.wasMainWindowVisible (was incorrectly or'd with wasSettingsVisible/wasModelSelectorVisible, which caused the main window to appear after a screenshot when the user had intentionally hidden it via Cmd+B while settings was open)
- [electron/CropperWindowHelper.ts]: senior review fix — hideOrClose() now hides (not closes) on macOS, matching Windows behavior; macOS previously called close() which destroyed the window after every use, forcing a 1-2s cold-start URL reload on every subsequent selective screenshot
- [electron/main.ts]: senior review fix — cropperWindowHelper.preload() now called on both win32 and darwin; macOS selective screenshot uses the cropper but was not preloaded, causing a cold-start delay on first use

- [electron/llm/prompts.ts]: added CODE_HINT_PROMPT — senior code reviewer prompt that gives 1-3 sentence nudge-style hints without writing the full solution
- [electron/llm/CodeHintLLM.ts]: new file — CodeHintLLM class that streams code hints from screenshots using CODE_HINT_PROMPT
- [electron/llm/index.ts]: exported CodeHintLLM and CODE_HINT_PROMPT
- [electron/IntelligenceEngine.ts]: added 'code_hint' to IntelligenceMode; added codeHintLLM field; initialized in initializeLLMs(); added runCodeHint(imagePaths?) method that streams via suggested_answer_token
- [electron/IntelligenceManager.ts]: added runCodeHint() pass-through to engine
- [electron/ipcHandlers.ts]: added 'generate-code-hint' IPC handler using getScreenshotQueue() for images
- [electron/preload.ts]: added generateCodeHint to ElectronAPI interface and contextBridge
- [src/types/electron.d.ts]: added generateCodeHint type declaration
- [electron/services/KeybindManager.ts]: added chat:codeHint keybind (Cmd+6)
- [src/hooks/useShortcuts.ts]: added codeHint to ShortcutConfig, DEFAULT_SHORTCUTS, mapBackendToFrontend, and updateShortcut switch
- [electron/main.ts]: added chat:codeHint to stealth shortcut dispatch map
- [src/components/NativelyInterface.tsx]: added handleCodeHint(), wired into handlersRef, and added 'codeHint' action handler in global shortcut listener
- [src/components/Queue/QueueCommands.tsx]: added onCodeHint prop and 💡 Hint button (shown when screenshots present, ⌘6)
- [src/components/Solutions/SolutionCommands.tsx]: added onCodeHint prop and 💡 Hint button (⌘6)
- [src/_pages/Queue.tsx]: added handleCodeHint() and passed to QueueCommands
- [src/_pages/Solutions.tsx]: added handleCodeHint() and passed to SolutionCommands
- [src/components/NativelyInterface.tsx]: fix — added codeHint to local keydown handler destructure and isShortcutPressed chain so Cmd+6 works when the window is focused (not just in stealth/background mode)
- [src/_pages/Queue.tsx]: fix — handleCodeHint converted to async with try/catch; was a fire-and-forget void with no error handling
- [src/_pages/Solutions.tsx]: fix — handleCodeHint converted to async with try/catch; same issue
- [src/components/Solutions/SolutionCommands.tsx]: fix — updated Hint button tooltip to clarify workflow (screenshot code first, then get hint); button intentionally always visible in solutions view
- [src/components/SettingsOverlay.tsx]: fix — added codeHint ("Get Code Hint") and brainstorm ("Brainstorm Approaches") entries to the Chat keybinds list so users can remap them via the Settings UI; both were registered in KeybindManager and useShortcuts but had no KeyRecorder row

- [electron/llm/prompts.ts]: Brainstorm Mode — added BRAINSTORM_MODE_PROMPT: 2-3 spoken problem-solving approaches with trade-offs, speakable in 60s, natural first-person speech, no code output
- [electron/llm/BrainstormLLM.ts]: new file — BrainstormLLM class that streams brainstorm approaches using conversation context + optional screenshots
- [electron/llm/index.ts]: exported BrainstormLLM and BRAINSTORM_MODE_PROMPT
- [electron/IntelligenceEngine.ts]: added 'brainstorm' to IntelligenceMode; added brainstormLLM field; initialized in initializeLLMs(); added runBrainstorm(imagePaths?) method that fetches 120s context, streams via suggested_answer_token with title "Brainstorming Approaches"
- [electron/IntelligenceManager.ts]: added runBrainstorm() pass-through to engine
- [electron/ipcHandlers.ts]: added 'generate-brainstorm' IPC handler using getScreenshotQueue() for images
- [electron/preload.ts]: added generateBrainstorm to ElectronAPI interface and contextBridge
- [src/types/electron.d.ts]: added generateBrainstorm type declaration
- [electron/services/KeybindManager.ts]: added chat:brainstorm keybind (Cmd+7)
- [src/hooks/useShortcuts.ts]: added brainstorm to ShortcutConfig, DEFAULT_SHORTCUTS, mapBackendToFrontend, and updateShortcut switch
- [electron/main.ts]: added chat:brainstorm to stealth shortcut dispatch condition and actionMap
- [src/components/Solutions/SolutionCommands.tsx]: added onBrainstorm prop and 🧠 Brainstorm button (⌘7, after Hint button)
- [src/components/Queue/QueueCommands.tsx]: added onBrainstorm prop and 🧠 Brainstorm button (⌘7, always visible)
- [src/_pages/Solutions.tsx]: added handleBrainstorm() and passed to SolutionCommands
- [src/_pages/Queue.tsx]: added handleBrainstorm() and passed to QueueCommands
- [src/components/NativelyInterface.tsx]: added handleBrainstorm(), wired into handlersRef, added 'brainstorm' action in global shortcut listener
- [src/components/NativelyInterface.tsx]: fix — added handleBrainstorm to local handleKeyDown destructure and isShortcutPressed chain (Cmd+7 was only firing via the stealth global path, not when the window was focused)

- [electron/llm/prompts.ts]: replaced BRAINSTORM_MODE_PROMPT with interview-focused "thinking out loud" script: brute-force → optimal pivot, bolded **Time/Space complexities**, ends with buy-in question
- [electron/llm/BrainstormLLM.ts]: simplified generateStream() to pass context directly to streamChat() (matching RecapLLM pattern), with early return guard when context + imagePaths are both empty
- [electron/services/SettingsManager.ts]: added actionButtonMode?: 'recap' | 'brainstorm' to AppSettings; defaults to 'recap' when undefined
- [electron/ipcHandlers.ts]: added get-action-button-mode and set-action-button-mode handlers; set handler broadcasts 'action-button-mode-changed' to all windows; fixed generate-brainstorm return key to { brainstorm } (was { result })
- [electron/preload.ts]: added getActionButtonMode, setActionButtonMode, onActionButtonModeChanged to ElectronAPI interface and contextBridge; updated generateBrainstorm return type to { brainstorm }
- [src/types/electron.d.ts]: added getActionButtonMode, setActionButtonMode, onActionButtonModeChanged type declarations; updated generateBrainstorm return type
- [electron/services/KeybindManager.ts]: renamed chat:recap → chat:dynamicAction4 with label 'Recap / Brainstorm', accelerator unchanged (Cmd+4)
- [src/hooks/useShortcuts.ts]: renamed recap → dynamicAction4 in ShortcutConfig interface, DEFAULT_SHORTCUTS, mapBackendToFrontend mapping, and updateShortcut switch
- [electron/main.ts]: replaced chat:recap with chat:dynamicAction4 in stealth shortcut condition and actionMap; removed old chat:recap entries
- [src/components/SettingsOverlay.tsx]: updated Chat keybinds list: recap row renamed to { id: 'dynamicAction4', label: 'Recap / Brainstorm' }
- [src/components/NativelyInterface.tsx]: added GitBranch icon import; added actionButtonMode state ('recap'|'brainstorm') with IPC load + subscription; added handleDynamicAction4() that delegates to handleRecap or handleBrainstorm based on mode; wired handleDynamicAction4 into handlersRef, local keydown (isShortcutPressed 'dynamicAction4'), global shortcut listener ('dynamicAction4' action); updated quick-action Recap button to show dynamic Brainstorm/Recap label+icon
- [src/components/SettingsPopup.tsx]: added actionButtonMode state + IPC load/subscribe; added "Interview Mode (Brainstorm)" toggle row (violet accent) between Transcript and Profile Mode toggles; calls setActionButtonMode IPC on toggle
- [electron/SessionTracker.ts]: added detectedCodingQuestion state (with source tag: 'screenshot'|'transcript'), setCodingQuestion()/getDetectedCodingQuestion()/clearCodingQuestion() methods, looksLikeCodingQuestion() heuristic (2+ pattern match + length≥50), auto-detection on final interviewer transcript segments, reset() clears question
- [electron/llm/prompts.ts]: replaced static CODE_HINT_PROMPT body with context-aware rules (cross-check code vs question, call out mismatch); added buildCodeHintMessage(questionContext, questionSource, transcriptContext) function that builds three dynamic message variants: with question, with transcript only, or with nothing
- [electron/llm/CodeHintLLM.ts]: updated generateStream() to accept questionContext, questionSource, and transcriptContext; calls buildCodeHintMessage to build rich user message instead of the old hardcoded string
- [electron/llm/index.ts]: exported buildCodeHintMessage
- [electron/IntelligenceEngine.ts]: runCodeHint() now accepts optional problemStatement; resolves question context from priority chain (problemStatement → session.detectedCodingQuestion → null); passes questionContext+source+transcriptContext to CodeHintLLM; logs resolution path
- [electron/IntelligenceManager.ts]: updated runCodeHint() signature to pass problemStatement; added setCodingQuestion(), getDetectedCodingQuestion(), clearCodingQuestion() pass-throughs
- [electron/ProcessingHelper.ts]: at problem extraction time, store problem_statement in intelligence session via setCodingQuestion('screenshot') so Code Hint has the question even when user screenshots only their code editor later
- [electron/ipcHandlers.ts]: updated generate-code-hint to pull appState.getProblemInfo() and pass problem_statement to runCodeHint; also syncs to session via setCodingQuestion; added get-detected-question and set-coding-question handlers
- [electron/preload.ts]: added getDetectedQuestion and setCodingQuestion to interface and contextBridge
- [src/types/electron.d.ts]: added getDetectedQuestion and setCodingQuestion types
- [src/components/SettingsPopup.tsx]: Senior review fix — optional-chain crash: `?.().then(` → `?.()?.then(` so undefined return from getActionButtonMode doesn't throw synchronously
- [electron/services/KeybindManager.ts]: Senior review fix — added ID_MIGRATIONS map in load() to remap saved 'chat:recap' → 'chat:dynamicAction4', preserving user-customized keybinds across the rename
- [electron/IntelligenceEngine.ts, electron/IntelligenceManager.ts, electron/ipcHandlers.ts]: Senior review fix — runBrainstorm now accepts optional problemStatement, injected as <problem_statement> block in context; IPC handler pulls getProblemInfo() and setCodingQuestion() same as code-hint
- [electron/ipcHandlers.ts]: Senior review fix — set-action-button-mode validates mode is 'recap' | 'brainstorm' before writing to settings

- [electron/SessionTracker.ts]: Architectural fix 1 — added codingQuestionSetAt timestamp field to track question age
- [electron/SessionTracker.ts]: Architectural fix 2 — replaced "screenshot always wins forever" rule with time-based staleness logic: transcript can override a screenshot question after 3 minutes (SCREENSHOT_STALE_MS), enabling multi-question interview sessions where Q1 screenshot no longer permanently blocks Q2 transcript detection
- [electron/SessionTracker.ts]: Architectural fix 3 — added recentInterviewerBuffer (5-min rolling window) for multi-segment question detection; interviewer stating a problem across multiple speech segments now triggers setCodingQuestion on the combined accumulated text, not just single segments
- [electron/SessionTracker.ts]: clearCodingQuestion() and reset() now also clear codingQuestionSetAt and recentInterviewerBuffer
- [electron/ipcHandlers.ts]: Architectural fix 4 — generate-code-hint handler now performs on-demand screenshot extraction if no problemStatement is available (user pressed ⌘6 without ever pressing ⌘↵ Solve); calls analyzeImageFiles on the current screenshot queue and caches the result in session via setCodingQuestion; subsequent ⌘6 presses reuse the cached extraction
- [electron/ipcHandlers.ts]: Critical fix — generate-code-hint and generate-brainstorm now auto-take a fresh screenshot (restoreFocus=false) at the top of the handler before reading the queue; previously the queue could contain a stale problem screenshot from an earlier ⌘H press, meaning the AI never saw the user's actual current code — it was essentially blind to what the user was writing

- [src/index.css]: Light mode palette overhaul — shifted from bright white-based iOS palette to comfortable Notion/Linear-style neutral grays; --bg-primary #EBEBEB (medium neutral page), --bg-secondary #DEDEDE (header chrome), --bg-elevated #F4F4F4 (cards — comfortable off-white), --bg-card #F4F4F4, --bg-item-active #CECECE, --bg-toggle-switch #BBBBBB, --text-primary #1A1A1A, accent changed from #007AFF (electric iOS blue) to #1565C0 (deep confident blue); overlay-bg and liquid-glass updated to match softer palette
- [src/components/Launcher.tsx]: Comprehensive light mode fixes — imported useResolvedTheme; detectable toggle pill hardcoded bg-[#101011] → bg-bg-elevated/shadow in light; Ghost/SVG icon fill="white" → theme-conditional fills; label text-[#B7B7B8] → text-text-secondary; toggle track bg-zinc-700 → bg-bg-toggle-switch; nav button white hover glows → dark shadows in light mode; refresh button hover white/10 → bg-bg-item-surface; context menu dropdown bg-[#1E1E1E]/80 → bg-bg-elevated with shadow; next meeting / prepared card text-white titles → text-text-primary; "Prepare"/"Start now" buttons theme-adaptive; context menu item hovers → bg-item-surface; Ollama status pill → semantic tokens; notification toast → theme-aware glass; undetectable dashed border border-white/20 → border-black/15 in light

- [src/types/solutions.ts, src/types/index.tsx]: replaced Solution interface with 4-phase interview teleprompter shape: problem_identifier_script, brainstorm_script, code, dry_run_script, time_complexity, space_complexity
- [electron/LLMHelper.ts]: replaced generateSolution() prompt with "Rolling Interview Script" system prompt; returns the 4-phase JSON directly (no solution wrapper); added regex JSON extraction fallback for model markdown fences
- [electron/ProcessingHelper.ts]: queue mode now calls generateSolution() after analyzeImageFiles() and sends SOLUTION_SUCCESS; changed validation_type/subtype to "coding" so Solutions.tsx renders the teleprompter view
- [src/_pages/Solutions.tsx]: complete UI overhaul — replaced split state vars with single solutionData: Solution | null; added PhaseCard, SpokenScript, ComplexityPill components; 4-phase teleprompter layout (Understand/Brainstorm/Implement/Verify) with colour-coded borders; spoken scripts rendered in italic quoted text; code block unchanged with SyntaxHighlighter; complexity pills in Verify phase

- [src/components/MeetingDetails.tsx]: light mode fix — added force-black-icon to the AI response logo img; the white PNG with only grayscale class was invisible on the light bg-bg-input container background; force-black-icon applies brightness(0) in light mode making it a visible dark icon

- [src/components/NativelyInterface.tsx]: increased message bubble max-width from 72.25% (user) / 85% (system) / 85% (live transcript preview) to 90% across all three — reduces wasted horizontal space and makes content less congested
- [src/index.css]: Redesigned light mode color token system — layered depth hierarchy: dark body floor (--bg-main: #CBCBCF) → mid-tier floating cards (--bg-card: #E8E8EB, --bg-elevated: #E6E6E9) → lightest interactive inputs (--bg-input: #F2F2F5); adjusted all related tokens for consistent depth
- [src/components/SettingsOverlay.tsx]: Fixed hardcoded dark values in ProviderSelect dropdown (border-white/5→border-border-subtle, bg-white/10→bg-bg-item-active, hover:bg-white/5→hover:bg-bg-item-surface, text-white→text-text-primary for labels); fixed Manage Pro button and Google sign-in button to use semantic tokens

- [electron/WindowHelper.ts]: added isCodeExpanded/preCodeExpansionBounds fields; added expandForCode(expand) public method — expands active window by 40% total (20% left + 20% right) with animated transition, clamps to workArea, restores original bounds on collapse; modified setOverlayDimensions to preserve expanded width while code is shown
- [electron/ipcHandlers.ts]: added expand-for-code IPC handler delegating to WindowHelper.expandForCode()
- [electron/preload.ts]: added expandForCode(expand: boolean) to ElectronAPI interface and contextBridge
- [src/types/electron.d.ts]: added expandForCode type declaration
- [electron/WindowHelper.ts, electron/ipcHandlers.ts, electron/preload.ts, src/types/electron.d.ts]: removed entire expandForCode IPC approach
- [electron/WindowHelper.ts]: setOverlayDimensions rework — symmetric x (widthDelta/2 left+right), aspect-ratio height (when widthRatio≥1.2 or ≤0.85, scale height by same ratio so window expands proportionally top-fixed/bottom-down), native macOS setBounds(bounds, true) animation only on significant width changes (>50px delta); height-only updates stay instant
- [src/components/NativelyInterface.tsx]: stutter fix — removed CSS width transition (was firing ResizeObserver ~21×/animation = 21 setBounds calls); CSS now instant (600/900px); single ResizeObserver call → single setBounds(animate=true) = Apple-smooth native animation; removed rAF debounce (no longer needed)
- [src/index.css]: Full light mode redesign — 'Warm Zinc' palette; three-tier depth system (body #E3E3E7 → cards #EBEBEE → inputs #F9F9FB); zinc-900/600/400 text scale; updated overlay token system and liquid-glass surface for light mode
- [src/index.css]: Complete light mode redesign using 'Warm Stone' palette (Tailwind stone family) — warm brownish-gray tones, 3-tier depth system (body #DDDAD7 → cards #EAE8E5 → inputs #F9F7F5), stone-900/600/400 text scale, blue-600 accent, updated overlay tokens and liquid-glass surface
- [src/index.css]: Full Apple HIG light mode redesign — exact Apple system colors (systemGray6 #F2F2F7 canvas, white #FFFFFF cards, systemBlue #007AFF accent, systemGray3/4/5 for interactive states, label/secondaryLabel/systemGray text); Apple vibrancy for overlay (white-tinted 200% saturate blur); updated liquid-glass to proper Apple frosted glass; added saturate(200%) overlay surfaces for light mode
- [src/index.css]: Research-backed light mode redesign — Professional SaaS standard (Linear/Vercel/Stripe patterns): #F5F5F5 canvas, #FFFFFF white cards + shadow elevation system, Tailwind gray-900/500/400 text scale, rgba(0,0,0,0.07) hairline borders, blue-600 accent; added CSS shadow rules for card/modal/dropdown depth; light mode app-sidebar with frosted white blur
- [src/components/SettingsOverlay.tsx]: light mode fixes — "General settings" rows now wrapped in a white card container with divide-y dividers and px-4 py-4 row padding (rows were floating naked on the canvas); opacity slider card and Process Disguise card changed bg-bg-item-surface → bg-bg-card so they surface correctly as white cards against the gray canvas
- [src/components/Launcher.tsx]: inverted depth fix — top section changed bg-bg-elevated → bg-bg-primary (gray canvas), hero cards changed bg-bg-secondary → bg-bg-elevated (white); next-meeting card given shadow utility; correct depth: gray canvas → white cards
- [src/components/AboutSection.tsx]: Creator pill light mode fix — added useResolvedTheme; pill uses amber-100/amber-700/amber-300 in light (visible), yellow-400/10/yellow-300/yellow-400 in dark; was invisible pale yellow in light
- [src/components/SettingsPopup.tsx]: fast mode toggle track visibility fix — defaultToggleTrackClass off-state changed bg-black/10 → bg-black/[0.22] in light mode; bg-black/10 ≈ #E6E6E6 was nearly invisible against the popup background
- [src/components/MeetingDetails.tsx]: Summary/Transcript/Usage tab text contrast fix — inactive tab text changed text-text-tertiary → text-text-secondary (gray-500 instead of gray-400) for better readability in light mode
- [src/components/FeatureSpotlight.tsx]: reverted all light mode changes — restored to original dark-only design (dark gradient background, white/gold text, 85% opacity screenshot, no theme awareness)
- [electron/WindowHelper.ts, src/components/NativelyInterface.tsx]: fully removed earlier code-expansion attempts — clean slate before new implementation
- [src/components/NativelyInterface.tsx]: auto-expanding code UI — shell div converted to motion.div with spring animation (stiffness:300, damping:30, mass:0.8) animating width 600↔900; isCodeExpanded state driven by latestMsgHasCode (useMemo on latest system message containing ```); contracts on scroll >50px via passive scroll listener on scrollContainerRef; scroll container max-h expands to clamp(450px,52vh,650px) when code is in view; ResizeObserver replaced with rAF-debounced version — during expand animation isMotionAnimatingRef suppresses all IPC calls (OS window pre-sized to 900px before spring starts), during contraction flag is clear so observer tracks the spring at ≤60fps; onAnimationComplete sends one final accurate IPC call
- [electron/WindowHelper.ts]: setOverlayDimensions — restored symmetric x-centering (adjustedX = x − widthDelta/2) so the OS window stays centre-anchored as it tracks the Framer Motion spring during both expand and contract
- [src/components/AboutSection.tsx]: dark mode regression fix — restored original dark pill to text-yellow-200/border-yellow-400/5 (was incorrectly changed to text-yellow-300/border-yellow-400/20 when scoping light mode)
- [src/components/MeetingDetails.tsx]: dark mode regression fix — added useResolvedTheme; inactive tab text now uses text-text-secondary only in light mode, text-text-tertiary in dark (restores original #606060 dim text in dark mode; no Tailwind darkMode strategy so dark: classes are inert and CSS vars control both modes)
- [src/components/Launcher.tsx]: dark mode regression fix — top hero section restored bg-bg-elevated (#151515) in dark (was changed to bg-bg-primary/#000000 making it same color as meetings list section below); meeting cards restored bg-bg-secondary (#050505) in dark (was bg-bg-elevated/#151515); both now use isLight conditional
- [src/components/SettingsOverlay.tsx]: dark mode regression fix — added isLight to main SettingsOverlay and ProviderSelect; general settings rows wrapper transparent in dark (was bg-bg-card/#242429, rows are now on original bg-bg-main background); section cards (Undetectable, Interface Opacity, Process Disguise) restored to bg-bg-item-surface in dark; ProviderSelect dropdown restored to bg-bg-elevated/90 border-white/5 in dark, selected items bg-white/10 and text-white in dark; Process Disguise option restored bg-zinc-800 text-white border-white/10 in dark; Calendar connect button restored bg-[#303033] text-white in dark
- [src/components/FeatureSpotlight.tsx]: light mode support — added useResolvedTheme/isLight; warm stone gradient background in light (#EEECEA→#E4E2DF) vs dark unchanged; screenshot opacity 20% in light (85% in dark); headline amber-800 in light vs gold #E6C46A in dark; subtitle/footer gray-700 in light vs soft white in dark; bullet color amber-800 in light; feature interest button dark-bordered in light; white gradient border hidden in light
- [src/components/ModelSelectorWindow.tsx]: light mode support — added useResolvedTheme/isLight; panel matches SettingsPopup light style (bg-[#F3F4F6]/92 border-black/10); selected item bg-black/[0.07] text-slate-900 in light; unselected text-slate-500 hover:bg-black/[0.04] in light; check icon text-emerald-600 in light
- [src/components/NativelyInterface.tsx]: replaced animate={{ width }} + state-driven expansion with useMotionValue + useSpring (animatedShellWidth) so width animation runs entirely on motion values — zero React re-renders per frame, eliminates card stutter; replaced latestMsgHasCode useMemo with checkCodeVisibility (getBoundingClientRect scan on [data-code-msg] elements in scroll container) for reliable expand/contract; added data-code-msg attribute to system message wrappers containing code fences; spring on('change') drives rAF-debounced IPC for OS window tracking
- [src/components/NativelyInterface.tsx]: fixed contraction not working — root cause was scroll listener effect using [checkCodeVisibility] dep so it ran once at mount when scrollContainerRef was null (conditionally rendered div); fix: added messages to dep array so listener re-attaches whenever scroll container first renders; removed isCodeExpanded state entirely — replaced with useTransform(animatedShellWidth, [600,900], [450,650]) driving scrollMaxH directly so scroll container height tracks spring with zero React re-renders; added rAF defer on messages effect so DOM is settled before querySelectorAll; tightened spring to stiffness:350, damping:32, mass:0.7; added willChange:width hint on shell
- [src/components/FeatureSpotlight.tsx]: reverted light mode changes — removed useResolvedTheme/isLight added in error; restored dark-only design (gradient from-[#1C1C1E] to-[#151516], screenshot opacity-85, black/20 overlay, gold/white text, always-shown gradient border)
- [premium/src/JDAwarenessToaster.tsx]: light mode support — added useResolvedTheme/isLight; white card bg with black/[0.08] border in light; headings text-[#1C1C1E] in light; body text black/50 in light; "Without" demo bubble bg-black/[0.03] in light; badge bg-[#F0F0F2] in light; dismiss button black/30 in light; overlay bg-black/30 in light
- [premium/src/ProfileFeatureToaster.tsx]: light mode support — same pattern as JDAwarenessToaster; white card, dark text, lighter demo bubble, badge bg-[#F0F0F2], dismiss button black/30 in light
- [premium/src/PremiumPromoToaster.tsx]: light mode support — added useResolvedTheme/isLight; bg-[#F8F8FA] card in light; black text headings/body; prism icon text-[#1C1C1E]; membership card bg-black/[0.03] border; pricing text dark; CTA button bg-[#1C1C1E] text-white in light; dismiss text black/20 in light
- [premium/src/RemoteCampaignToaster.tsx]: light mode support — same approach; bg-[#F8F8FA] card; bg-[#121826]→bg-[#F8F8FA]; all text/border conditionals for light; prism visual dark in light; promo block text dark; CTA blue gradient unchanged; dismiss text black/20 in light
- [src/components/NativelyInterface.tsx]: changed code expansion from 1.5x (900px) to 1.3x (780px); updated scrollMaxH transform range to [450,585] accordingly
- [src/components/NativelyInterface.tsx]: fixed animation stutter root cause — removed animatedShellWidth.on('change') IPC loop that was calling setBounds 60fps (every macOS setBounds forces full window-server recomposite); replaced with 2-call strategy: pre-size OS window immediately on expand (spring animates inside already-correct window), post-size on contract after spring settles; added translateZ(0)+willChange:transform GPU layer hints on shell; tightened spring to stiffness:400 damping:38 mass:0.8 for faster settle; extracted syncOSWindow helper
- [src/main.tsx, src/hooks/useResolvedTheme.ts, index.html]: fixed light/dark mixed theme bug — root cause: getThemeMode() IPC is async so data-theme was set after React rendered, causing useResolvedTheme to initialize as 'dark' regardless of actual theme; fix: (1) cache resolved theme in localStorage, apply it synchronously before ReactDOM.createRoot so useState initial read is correct; (2) useResolvedTheme now uses MutationObserver on data-theme attribute to catch the async authoritative correction; (3) index.html body background changed from hardcoded #000000 to transparent to eliminate black flash in light mode
